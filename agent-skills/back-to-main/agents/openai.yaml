interface:
  display_name: "Back To Main"
  short_description: "Fetch with prune, switch to main, and pull latest changes."
  default_prompt: "Use $back-to-main to return this repository to an up-to-date main branch safely."
