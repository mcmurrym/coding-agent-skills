interface:
  display_name: "Linear Start Work"
  short_description: "Start work from a Linear issue fast"
  default_prompt: "Use this skill to start work from a Linear issue link or key: load full issue context, including comments, attachments, parent issue context when present, and child sub-issues; then create and push a branch from the issue slug and move it to In Progress."
