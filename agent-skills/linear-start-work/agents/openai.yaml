interface:
  display_name: "Linear Start Work"
  short_description: "Start and implement a Linear issue"
  default_prompt: "Use $linear-start-work to start work from a Linear issue link or key: load full context, research the repo, create and push a branch when appropriate, move the issue to In Progress, then implement and validate the issue unless genuinely blocked."
