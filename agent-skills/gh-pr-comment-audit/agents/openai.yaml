interface:
  display_name: "GH PR Comment Audit"
  short_description: "Triage PR review comments — auto-fix trivials, resolve non-issues, surface complex ones"
  default_prompt: "Use $gh-pr-comment-audit to fetch PR review comments, evaluate each finding, auto-fix trivial issues, resolve non-real threads on GitHub, and present complex issues in chat for decision."
