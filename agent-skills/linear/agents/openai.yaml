interface:
  display_name: "Linear"
  short_description: "Manage Linear issues and projects"
  icon_small: "./assets/linear-small.svg"
  icon_large: "./assets/linear.png"
  default_prompt: "Use $linear to triage or update the relevant Linear issues for this task, with clear next actions."

dependencies:
  tools:
    - type: "mcp"
      value: "linear"
      description: "Linear MCP server"
      transport: "streamable_http"
      url: "https://mcp.linear.app/mcp"
