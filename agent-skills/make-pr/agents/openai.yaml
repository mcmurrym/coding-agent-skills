interface:
  display_name: "Make PR"
  short_description: "Create PRs with branch summaries"
  default_prompt: "Use $make-pr to create a PR from the current branch with a concise summary, then render a Codex HTML button whose follow-up contains only the $gh-pr-comment-audit skill callout."
